/*
This file holds a demo component for testing Azure Text-to-Speech (TTS) functionality in the browser. It allows you to synthesize speech using Azure’s TTS API, but is not used in the main app flow, which relies on Eleven Labs for production TTS features.
*/

import Comentary from "./comentary-ui";
const Page = () => <Comentary />;

export default Page;
